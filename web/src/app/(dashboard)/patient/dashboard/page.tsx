import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function PatientDashboardPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Patient Dashboard - Coming Soon</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">This module will include appointments, records, and messaging.</p>
      </CardContent>
    </Card>
  );
}
